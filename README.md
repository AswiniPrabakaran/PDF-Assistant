# PDF-Assistant
Built an AI PDF Question &amp; Answer Assistant using React and FastAPI. Implemented a basic RAG pipeline to extract PDF text, create embeddings, retrieve relevant content, and generate answers to user queries using an LLM.
